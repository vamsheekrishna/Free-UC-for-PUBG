<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="enrollViewItem"
            type="com.example.myapplication.models.EnrollViewDataItem" />
    </data>
    <RelativeLayout
        android:background="@android:color/transparent"
        android:layout_width="match_parent"
        android:layout_height="190dp"
        android:gravity="center"
        android:visibility="visible"
        android:layout_margin="16dp"
        android:layout_gravity="center"
        tools:visibility="visible">


        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="200dp">
            <View
                android:background="@drawable/home_icon_bg"
                android:layout_marginTop="40dp"
                android:layout_marginBottom="16dp"
                android:layout_width="match_parent"
                android:layout_height="match_parent"/>
        </LinearLayout>
        <ImageView
            android:id="@+id/logo"
            android:layout_centerHorizontal="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:scaleType="centerInside"
            app:imageResource="@{enrollViewItem.image}"/>
        <TextView
            android:id="@+id/menu_text"
            android:layout_below="@+id/logo"
            android:layout_marginTop="8dp"
            android:layout_gravity="center"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/gray"
            android:textSize="20sp"
            android:text="@={enrollViewItem.name}"
            android:layout_centerHorizontal="true"
            android:textStyle="italic|bold"/>
        <Button
            android:id="@+id/submit"
            android:text="Free UC"
            android:layout_marginTop="4dp"
            android:textColor="@color/white"
            android:paddingStart="32dp"
            android:paddingEnd="32dp"
            android:layout_below="@+id/menu_text"
            android:layout_centerHorizontal="true"
            android:background="@drawable/sign_up_submit_bg"
            android:layout_width="wrap_content"
            android:layout_height="32dp"/>

        <!---->
    </RelativeLayout>
    <!--<LinearLayout
        android:gravity="left"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:layout_margin="8dp"
        android:background="@drawable/registration_bg">
        <ImageView
            android:scaleType="centerInside"
            android:layout_width="150dp"
            android:layout_height="150dp"
            app:imageResource="@{enrollViewItem.image}">
        </ImageView>

    </LinearLayout>-->
</layout>